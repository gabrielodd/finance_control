<%= form_with(model: despesa, local: true) do |form| %>
  <% if despesa.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(despesa.errors.count, "error") %> prohibited this despesa from being saved:</h2>

      <ul>
        <% despesa.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :descricao %>
    <%= form.text_field :descricao %>
  </div>

  <div class="field">
    <%= form.label :valor %>
    <%= form.text_field :valor %>
  </div>

  <div class="field">
    <%= form.label :categoria %>
    <%= form.select(:categoria_id, Categoria.all.map { |c| [c.name, c.id] }) %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
